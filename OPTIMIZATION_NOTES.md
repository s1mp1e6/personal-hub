# 优化记录与下一步建议

## 已完成

- 修复点击“每日激励”后时钟短暂显示 `--:--` 的问题。
- “每日激励”点击改为局部更新，不再重绘整个桌面，减少闪动和滚动位置变化。
- 桌面渲染后立即刷新时钟，避免所有桌面重绘场景出现占位时间。
- 移除 `user-scalable=no`，允许手机和平板用户缩放。
- 增加手机断点：卡片、时间轴、弹窗、侧栏底部按钮、悬浮按钮适配小屏。
- 保存逻辑改为 200ms 防抖，减少连续操作时的 IndexedDB 写入压力。
- 页面隐藏前立即保存，降低刚编辑完关闭页面时的数据丢失风险。
- 激励语至少保留一条，避免空数组导致状态异常。
- 加入 Playwright 自动化验证脚本，覆盖桌面、平板、手机、小屏手机。

## 自动化验证

在 `test-tools/` 中运行：

```bash
set PLAYWRIGHT_BROWSERS_PATH=F:\GitHub-Trending\10-Personal-Hub\test-tools\ms-playwright
npm run scan
npm run verify
npm run audit
npm run verify:sync
```

验证内容：

- 页面可加载。
- 点击“每日激励”后时钟不是占位文本。
- 桌面、平板、手机、小屏手机没有横向溢出。
- 控制台没有错误。
- 截图输出到 `verification/`。
- 静态扫描覆盖：HTML/Service Worker 语法、`document.write`、生产残留 `console.log`、替换乱码字符、`target="_blank"` 安全属性。

交互体检覆盖：

- 桌面端：每日激励、备份说明、导出备份、激励语删除保护、添加待办、勾选待办。
- 移动端：侧栏打开/关闭、模块导航、文献弹窗、手写输入入口、返回桌面、每日激励。

## 本轮实测发现

- 移动端侧栏关闭动画容易在截图/快速操作中留下视觉残影，已增加遮罩层和统一关闭函数。
- 自动化测试中多个“删除”“待办事项”同名按钮会造成选择歧义。真实用户可理解，但后续做无障碍时应补充更明确的 `aria-label`。
- 当前 OCR 依赖外部 CDN，离线或网络差时不可用。后续可在界面里明确“联网 OCR”状态，或延后加载时给更稳定的提示。
- 本轮已补充关键按钮 `aria-label`、外链 `noopener`、移除图片预览中的 `document.write`，并给 OCR 增加联网提示、空图片提示和失败兜底。
- 已加入主题切换：默认（原始）、暖白、清爽、深色、森绿。“默认（原始）”表示不设置 `data-theme`，保留最初原始样式；旧版误设为 `warm` 且未显式选择过主题的记录会自动迁回默认。
- 已加入本机备份文件夹能力：默认仍使用浏览器 IndexedDB 和下载备份；支持 File System Access API 的浏览器可手动授权一个文件夹，导出时优先写入该文件夹。不支持的浏览器自动退回下载 JSON。
- 新上传附件改为 IndexedDB Blob 存储，主数据只保存文件引用，避免图片/文档继续把主 JSON 撑大。
- 导出、导入和 WebRTC 同步会打包/恢复 Blob 附件。
- 已加入模块搜索、显示归档、待办/任务一键归档已完成项。
- 已加入 WebRTC 近距离同步实验版：当前使用复制/粘贴配对码建立点对点连接，避免引入第三方二维码服务导致配对数据泄露。
- 已修复配对码生成慢的问题：ICE 收集最多等待 1.5 秒，并在界面中持续显示状态。
- WebRTC 发送改为分片传输，避免完整备份较大时一次性发送失败。
- 配对码改为 LZ 压缩格式 `ph1.`，长度更短，二维码更容易显示和识别。
- 摄像头扫码增加 HTTPS 检查、后置摄像头优先、普通摄像头兜底、黑屏提示和复制/粘贴退路。
- 已加入双页面同步验证脚本 `npm run verify:sync`。

## 后续高价值优化

- 设备联动：已加入本地二维码生成和摄像头扫码识别；复制/粘贴配对码仍作为兜底。
- 移动端扫码需要浏览器授予摄像头权限；自动化环境无法验证真实摄像头授权，但已验证同步弹窗、二维码 SVG 生成、双页面 WebRTC 连接和备份传输。
- 数据合并：当前接收同步仍是覆盖导入，下一步支持“只合并新增数据”。
- 附件治理：继续做附件管理页，显示附件数量、体积、未引用文件清理。
- 可访问性：继续补键盘快捷操作，例如 Enter 打开卡片、Escape 关闭侧栏/菜单。
- 性能：长列表时增加搜索、筛选或虚拟滚动。
- 数据安全：导入前显示备份时间、大小、模块数量，让用户确认来源。
- 发布体验：Cloudflare Pages 更适合作为正式免费托管入口，GitHub Pages 可保留为镜像。
